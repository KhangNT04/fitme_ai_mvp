# FitMe AI — Cloud deploy (Vercel + Render + Neon)
# Copy sections below into each platform's dashboard. Do NOT commit real secrets.

# ── Neon (PostgreSQL) ─────────────────────────────────────────
# Create project at https://neon.tech → Connection string (direct, not pooled for Flyway)
# Example JDBC (append sslmode=require if missing):
# DB_URL=jdbc:postgresql://ep-xxxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
# DB_USERNAME=neondb_owner
# DB_PASSWORD=your-neon-password

# ── Render (Spring Boot backend) ────────────────────────────
# Web Service → Docker → backend/Dockerfile
# Health check path: /actuator/health
# SPRING_PROFILES_ACTIVE=prod
# DB_URL=
# DB_USERNAME=
# DB_PASSWORD=
# JWT_SECRET=random-string-min-32-chars
# CORS_ORIGINS=https://fitme-ai-mvp.vercel.app
# FITME_SEED_ENABLED=false
# FITME_SEED_FASHION_REFRESH=true
# FITME_SEED_PASSWORD=strong-demo-password-only-if-seed-enabled
# UPLOAD_DIR=/tmp/uploads

# ── PayOS / Brand billing (Render only) ─────────────────────
# Giai đoạn test cloud (không cần key PayOS):
# PAYOS_MOCK=true
# PAYOS_RETURN_URL=https://fitme-ai-mvp.vercel.app/brand/billing/return?status=success
# PAYOS_CANCEL_URL=https://fitme-ai-mvp.vercel.app/brand/billing/return?status=cancel
#
# PayOS thật (bắt buộc trước khi lưu webhook trên my.payos.vn):
# PAYOS_MOCK=false
# PAYOS_CLIENT_ID=<my.payos.vn>
# PAYOS_API_KEY=<my.payos.vn>
# PAYOS_CHECKSUM_KEY=<my.payos.vn>
# PAYOS_RETURN_URL=https://fitme-ai-mvp.vercel.app/brand/billing/return?status=success
# PAYOS_CANCEL_URL=https://fitme-ai-mvp.vercel.app/brand/billing/return?status=cancel
# Webhook URL đăng ký trên my.payos.vn:
#   https://fitme-api.onrender.com/api/v1/webhooks/payos
#
# Render FREE — quy trình lưu webhook (tránh timeout 10s):
# 1. Set PAYOS_MOCK=false + 3 key ở trên → Manual Deploy
# 2. Mở https://fitme-api.onrender.com/actuator/health → đợi {"status":"UP"}
# 3. Trong 1–2 phút, my.payos.vn → dán webhook URL → Lưu
# 4. (Tùy chọn) UptimeRobot ping /actuator/health mỗi 5 phút để giảm cold start

# ── Vercel (Next.js frontend) ─────────────────────────────────
# Root Directory: frontend
# Framework: Next.js
# NEXT_PUBLIC_API_URL=/api/v1
# BACKEND_INTERNAL_URL=https://fitme-api.onrender.com
#   ↑ URL Render backend (no trailing slash), set AFTER first Render deploy
# JWT_SECRET=<same value as Render — required for portal middleware JWT verify>
# (Vercel không cần biến PAYOS_*)

# ── AI VTON (Render backend + ai-vton service) ───────────────
# Local/dev: FITME_AI_MODE=mock (sync mock preview)
# Production VTON: deploy ai-vton as 2nd Render Web Service (Docker ai-services/vton/Dockerfile)
# FITME_AI_MODE=hf
# AI_VTON_URL=https://fitme-ai-vton.onrender.com
# FITME_PUBLIC_BASE_URL=https://fitme-api.onrender.com
# FITME_FRONTEND_BASE_URL=https://fitme-ai-mvp.vercel.app
# HF_TOKEN=hf_xxx   (set on ai-vton service only — improves HF Space queue priority)
# HF_SPACE=yisol/IDM-VTON
# HF_MAX_RETRIES=3
# HF_RETRY_DELAY_SECONDS=5
# HF_FALLBACK_COMPOSITE=true
# VTON_PROVIDER_CHAIN=hf,replicate,composite
# REPLICATE_API_TOKEN=r8_xxx   (set on ai-vton service — fallback when HF Space fails)
# REPLICATE_IDM_VTON_MODEL=cuuupid/idm-vton:906425dbca90663ff5427624839572cc56ea7d380343d13e2a4c4b09d3f0c30f
# VTON_PUBLIC_BASE_URL=https://fitme-ai-vton.onrender.com

# ── AI Stylist (Gemini) — Render backend ─────────────────────
# FITME_AI_STYLIST_MODE=gemini
# GEMINI_API_KEY=AIza...
# GEMINI_MODEL=gemini-2.0-flash
# GEMINI_TIMEOUT_MS=25000

# ── Cloudflare R2 (Render backend — public user photos for VTON) ─
# FITME_STORAGE_MODE=r2
# R2_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
# R2_BUCKET=fitme-uploads
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_PUBLIC_BASE_URL=https://pub-xxxx.r2.dev

# ── Bật lại seed demo (tùy chọn) ─────────────────────────────
# Trên Render: FITME_SEED_ENABLED=true + FITME_SEED_PASSWORD=<mật khẩu mạnh>
# Chỉ có hiệu lực khi DB trống lần đầu hoặc top-up theo cấu hình seed.
