fitme:
  seed:
    enabled: false
    fashion-catalog-refresh: true
    top-up-enabled: true
  test:
    expose-reset-tokens: false

springdoc:
  swagger-ui:
    enabled: false
  api-docs:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
